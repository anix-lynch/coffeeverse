# Streamlit Cloud - Optimized Dependencies
# Removed heavy packages (azure-functions, azure-identity)
# Keeping only what Streamlit dashboard needs

streamlit>=1.28.0
plotly>=5.15.0
pandas>=2.0.0
pyarrow>=12.0.0
python-dotenv>=1.0.0

# Azure SDK - Minimal set for Cosmos DB + Blob Storage
azure-storage-blob>=12.19.0
azure-cosmos>=4.5.0
